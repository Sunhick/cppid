# Enable warnings
build --cxxopt  -std=c++14
build --cxxopt -Wall

# commenting Werror for now. how to ignore
# warnings from 3rd party packages. google-test.
# build --cxxopt -Werror

build --cxxopt -Wextra
build --cxxopt -Woverloaded-virtual
build --cxxopt -Wpedantic
build --cxxopt -Wsequence-point
build --cxxopt -Wunused
build --cxxopt -pedantic-errors
